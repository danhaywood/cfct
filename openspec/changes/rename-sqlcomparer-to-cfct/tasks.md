## 1. Naming updates

- [x] 1.1 Update user-facing project naming from `sqlcomparer` to `cfct` in scoped docs and usage text.
- [x] 1.2 Introduce or update first-mention expansion to `Command Footprint Comparison Tool` where appropriate.

## 2. Script rename

- [x] 2.1 Rename `comparedb.sh` to `cfct.sh`.
- [x] 2.2 Update script-internal usage/help text to the new script name if needed.
- [x] 2.3 Update repository references from `comparedb.sh` to `cfct.sh`.

## 3. Verification

- [x] 3.1 Run relevant tests or checks that cover script invocation and docs-linked flows.
- [x] 3.2 Validate no stale references to `comparedb.sh` remain in maintained files.
