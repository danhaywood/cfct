## 1. Naming updates

- [ ] 1.1 Update user-facing project naming from `sqlcomparer` to `cfct` in scoped docs and usage text.
- [ ] 1.2 Introduce or update first-mention expansion to `Command Footprint Comparison Tool` where appropriate.

## 2. Script rename

- [ ] 2.1 Rename `scripts/comparedb.sh` to `scripts/cfct.sh`.
- [ ] 2.2 Update script-internal usage/help text to the new script name if needed.
- [ ] 2.3 Update repository references from `scripts/comparedb.sh` to `scripts/cfct.sh`.

## 3. Verification

- [ ] 3.1 Run relevant tests or checks that cover script invocation and docs-linked flows.
- [ ] 3.2 Validate no stale references to `scripts/comparedb.sh` remain in maintained files.
