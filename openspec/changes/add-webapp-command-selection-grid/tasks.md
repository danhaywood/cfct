## 1. Command-grid data and state plumbing

- [x] 1.1 Add or wire a webapp-facing service that loads command-grid rows with interaction identifiers and display columns.
- [x] 1.2 Add view-model state for multi-command selection in the home view.
- [x] 1.3 Ensure selected command interactionIds are available to later comparison orchestration state.

## 2. Left-panel UI layout and command grid

- [x] 2.1 Add a command-selection grid section in the left navigation area above the existing table grid.
- [x] 2.2 Add filtering controls for visible command-grid columns with live filtering behavior.
- [x] 2.3 Add spacer treatment above the command-grid block to improve top-of-panel rhythm.
- [x] 2.4 Keep the existing manual table grid behavior and placement below the command grid.

## 3. Verification

- [x] 3.1 Add or update unit/component tests for command-grid rendering, ordering, and multi-selection behavior.
- [x] 3.2 Add or update browser-level tests for command-grid filtering and left-panel layout expectations.
- [x] 3.3 Run relevant webapp and integration test commands and confirm behavior is stable.
