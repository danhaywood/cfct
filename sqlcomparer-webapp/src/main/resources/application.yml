cfct:
  webapp:
    comparison:
      connection:
        server: localhost:14333
        username: sa
        password: change-me
        left-database: left_db
        right-database: right_db
      env-file: .env
      output:
        format: text
        file:
      validation:
        enabled: true
        fail-fast: true
    # Migration note: legacy keys removed
    # - cfct.webapp.comparison.table-selection.tables
    # - cfct.webapp.comparison.table-selection.tables-file
    selection-plan:
      explicit:
        tables:
          - dbo.Supplier
